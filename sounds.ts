// Sound functionality is temporarily disabled for debugging.
// This will be restored once the main application is running correctly.

/**
 * A placeholder function that does nothing.
 * @param soundName - The name of the sound file.
 * @param loop - Whether the sound should loop.
 */
export const playSound = (soundName: string, loop: boolean = false): void => {
    // Intentionally left blank.
    // console.log(`Sound disabled: Play request for '${soundName}'`);
};

/**
 * A placeholder function that does nothing.
 * @param soundName - The name of the sound to stop.
 */
export const stopSound = (soundName: string): void => {
    // Intentionally left blank.
    // console.log(`Sound disabled: Stop request for '${soundName}'`);
};
